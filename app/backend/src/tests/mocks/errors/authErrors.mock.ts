const authNoEmailOrPass = {
  message: 'All fields must be filled',
};

const authInvalidEmailOrPass = {
  message: 'Invalid email or password',
};

const authTokenNotFound = {
  message: 'Token not found',
};

const tokenNotValid = {
  message: 'Token must be a valid token',
};

export default {
  authNoEmailOrPass,
  authInvalidEmailOrPass,
  authTokenNotFound,
  tokenNotValid,
};
